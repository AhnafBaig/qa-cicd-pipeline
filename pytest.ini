[pytest]
addopts =
    -v
    --tb=short
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    smoke: Fast subset run on every push
    regression: Full suite run nightly
    ui: Playwright UI tests
    api: REST API tests
    critical: Must-pass tests that gate a release
